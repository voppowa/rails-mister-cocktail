<%= form_for [@cocktail, @dose] do |f| %>
 <%= f.label :description %>
 <%= f.text_field :description, class:"form-control" %>
 <%= f.select(:ingredient_id, options_for_select( Ingredient.all.pluck(:name, :id)))%>
 <%= f.submit "Submit", class:"form-control btn btn-primary" %>
<% end %>
